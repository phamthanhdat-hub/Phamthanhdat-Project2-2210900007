@model IEnumerable<K22CNT3_PhamThanhDat_Project2.Models.DanhMuc>

@{
    ViewBag.Title = "Index";
}

<h2>Pham Thanh Dat - Danh Muc</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenDanhMuc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenDanhMuc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaDanhMuc }, new { @class = "btn btn-success" }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaDanhMuc }, new { @class = "btn btn-info" }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaDanhMuc }, new { @class = "btn btn-danger"  })
        </td>
    </tr>
}

</table>
